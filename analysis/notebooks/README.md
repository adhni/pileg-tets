# Notebook Status

This directory is no longer the active analysis path.

The maintained workflow is Python-first under `analysis/python/`.

Legacy R/Quarto notebook sources were moved to `archive/r_reference/`.

Any HTML files left here are old rendered artifacts, not the source of truth.
